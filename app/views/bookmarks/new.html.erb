<h1>Add Movie to List</h1>
<%= form_for [@list, @bookmark] do |f| %>
  <%= f.label :movie_id %>
  <%= f.collection_select :movie_id, Movie.all, :id, :title %>
  <%= f.label :comment %>
  <%= f.text_area :comment %>
  <%= f.submit %>
<% end %>


